'use strict';

/**
 * contributions-type service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::contributions-type.contributions-type');

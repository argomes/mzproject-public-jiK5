'use strict';

/**
 * contributions-type controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::contributions-type.contributions-type');
